import { CircularProgress } from "@mui/material";
import { Main } from "../../style";

const Loader = () => {
  return (
    <Main>
      <CircularProgress />
    </Main>
  );
};

export default Loader;
